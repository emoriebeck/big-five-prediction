#!/bin/bash
#PBS -m n
## Give the job a name to help keep track of running jobs
#PBS -N sca_vlntrd_mvout
# specify the array
#PBS -t 1-2100%%750
# Specify the resources needed
#PBS -l nodes=1,vmem=3gb,walltime=96:00:00

## merge standard error and output
#PBS -j oe

# Use the old queue for testing
#PBS -q old_route

# copy the downloaded R script from your home directory to the current directory
# cp ~/sca_run.R 

# Typically processing will be done in a user's scratch directory

# usecp *:/home /home

# Load the environmental variables necessary for running R
module load R-3.6.1

export PARAMS=`cat ${HOME}/sca_args.txt |
  sed -n ${PBS_ARRAYID}p`

cd ~/selection

## Run R script based on the array number. 
Rscript $HOME/sca_run.R 
