#!/bin/bash
#PBS -m n
## Give the job a name to help keep track of running jobs
#PBS -N psm_predInt
# specify the array
#PBS -t 1-980%%300
# Specify the resources needed
#PBS -l nodes=1:ppn=8,mem=4gb,walltime=168:00:00

## merge standard error and output
#PBS -j oe

#PBS -q old_route

export PARAMS=`cat ${HOME}/psm_matched_args.txt |
  sed -n ${PBS_ARRAYID}p`

# Load the environmental variables necessary for running R
module load R-3.6.1

cat ~/.R/Makevars 

cd /scratch/edbeck

## Run R script based on the array number. 
Rscript psm_matched_run_old_pe8.R 
